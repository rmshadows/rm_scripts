# 备份说明

此目录为 **Debian GNOME 装机脚本** 的原始残片（来自 `Debian_GNOME_Init` 项目），
曾误放在 `fluxbox/setup/` 下，与 Fluxbox 配置部署无关。

完整装机项目路径：`../../Debian_GNOME_Init/`（相对 rm_scripts 仓库根目录）

备份时间：2026-06-16

包含文件：
- `Debian_12_Bookworm_GNOME_Setup.sh` — 原 GNOME 装机入口（需 0~8 子目录，此处不完整）
- `Config.sh` — GNOME/APT/输入法 等装机开关
- `GlobalVariables.sh`
- `Lib.sh`
