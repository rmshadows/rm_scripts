# 模板：off
# 进出全丢。应急断网用。恢复：sudo sw-rules 再选 normal 或 web。
# 选用：sudo sw-rules
# 改完：sudo shorewall check && sudo shorewall reload
#
# ACTION		SOURCE		DEST		PROTO	DEST	SOURCE
#							PORT	PORT(S)
?SECTION ALL
?SECTION ESTABLISHED
?SECTION RELATED
?SECTION INVALID
?SECTION UNTRACKED
?SECTION NEW
Invalid(DROP)	net		$FW		tcp

# ============================================================
# 全关（已启用）
# 需要留一条 SSH 救命时，取消下面 SSH 那行，并注释掉两条 DROP。
# ============================================================
DROP	net	$FW	all
DROP	$FW	net	all
# SSH(ACCEPT)	net		$FW
