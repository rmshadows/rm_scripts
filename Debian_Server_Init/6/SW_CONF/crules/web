# 模板：web
# 已开 SSH + HTTP/HTTPS。其余同 normal，取消注释即可。
# 选用：sudo sw-rules
# 改完：sudo shorewall check && sudo shorewall reload
#
# ACTION		SOURCE		DEST		PROTO	DEST	SOURCE
#							PORT	PORT(S)
?SECTION ALL
?SECTION ESTABLISHED
?SECTION RELATED
?SECTION INVALID
?SECTION UNTRACKED
?SECTION NEW
Invalid(DROP)	net		$FW		tcp

# ============================================================
# SSH（已启用）
# ============================================================
SSH(ACCEPT)	net		$FW

# ============================================================
# Ping
# ============================================================
Ping(DROP)	net		$FW
# Ping(ACCEPT)	net		$FW

# ============================================================
# HTTP / HTTPS（已启用：80 续期 + 443 站点）
# ============================================================
Web(ACCEPT)	net		$FW

# ============================================================
# 邮件
# ============================================================
# ACCEPT	net	$FW	tcp	25,465,587
# ACCEPT	net	$FW	tcp	110,143,993,995

# ============================================================
# 自定义 TCP 端口（改数字后取消注释）
# ============================================================
# ACCEPT	net	$FW	tcp	8080
# ACCEPT	net	$FW	tcp	8443
